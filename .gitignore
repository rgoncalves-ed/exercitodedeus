.DS_Store
Thumbs.db
node_modules/
.shopify/
*.log
.env
.env.local

# Build local / preview estático (não faz parte do tema)
_preview/

# Documentos internos de proposta e planejamento (não fazem parte do tema)
_proposta/

# Zips gerados localmente
*.zip

# Configuração local do Claude Code (máquina específica)
.claude/settings.local.json
.claude/worktrees/
